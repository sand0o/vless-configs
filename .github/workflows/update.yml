name: Update VLESS Configs

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    
    # Ключевое изменение - даем права на запись
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        # Важно: используем специальный токен
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Create or update config file
      run: |
        echo "# 🚀 VLESS Configurations" > vless.txt
        echo "# Updated: $(date '+%Y-%m-%d %H:%M:%S UTC')" >> vless.txt
        echo "" >> vless.txt
        echo "vless://160e9c18-c548-42bf-9c00-905aed8ef96f@subscribe.getvlessvpn.com:49000?mode=gun&security=reality&encryption=none&pbk=2K2NO2jXF45pA0-eUobqo-gnur61TadbWD4B93Zl_Vc&fp=chrome&type=grpc&serviceName=dropbox.com&sni=google.com&sid=170f8a2036#VPN" >> vless.txt
        echo "" >> vless.txt
        echo "" >> vless.txt
        echo "# Total: 2 configs" >> vless.txt
        
    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        # Добавляем все изменения
        git add .
        
        # Проверяем есть ли изменения для коммита
        if [[ -n $(git status --porcelain) ]]; then
          git commit -m "🤖 Auto-update: $(date '+%Y-%m-%d %H:%M')"
          git push
          echo "✅ Changes pushed successfully"
        else
          echo "📭 No changes to commit"
        fi
